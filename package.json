{
  "name": "pulse",
  "private": true,
  "version": "0.0.1",
  "description": "PULSE — the self-aware stadium. Gemini + ADK multi-agent operating layer for large-scale sporting venues.",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "deploy": "bash scripts/deploy.sh",
    "deploy:one": "bash scripts/deploy-one.sh",
    "seed": "tsx scripts/seed-firestore.ts"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.6.0",
    "tsx": "^4.19.0"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
